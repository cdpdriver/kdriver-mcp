# kdriver-mcp

MCP server to expose kdriver to LLMs

> This project is still a work in progress. It is not yet ready for production use.

## Start the server locally

```bash
./gradlew jvmJar && java -jar mcp/build/libs/mcp-jvm-0.1.0.jar
```
